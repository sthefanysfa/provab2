import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
        flex: 1,
        backgroundColor: '#e68ad5',
        alignItems: 'center',
        justifyContent: 'center',
    }

})
export default styles;